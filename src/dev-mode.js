// VIRUZ PET — Dev Mode temporarily disabled
// Emergency rollback: this file is intentionally inert while the game boot
// is being restored. Re-enable only after startup is confirmed stable.

export function applyDevMapPatch() {
  // no-op
}

export function wireDevMode() {
  // no-op
}
