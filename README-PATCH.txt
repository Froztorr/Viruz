VIRUZ PET — patch

Changed this round:
  src/engine.js   — opposed crit/eva model (point-based stats)

Copy src/engine.js into your repo's src/ folder, replacing the
existing file. Nothing else changed, so no other files are included.
